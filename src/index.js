import React from 'react';
import ReactDOM from 'react-dom/client';
import Randomify from './Randomify';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
	<React.StrictMode>
		<Randomify />
	</React.StrictMode>
);